# Xcode / build
build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local secrets — never commit
.env

# Claude Code local files (keep .claude/settings.json tracked — it holds shared permission rules)
/.claude/settings.local.json
CLAUDE.local.md
